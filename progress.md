# Search progress

Search cursors and processed sets were reset for the behavior-fingerprint
strategy. The owner-profile cache was retained. The next scheduled workflow run
will seed `users → identity → personal → site`.
